var sum = function add(x) {
		var count = 0;
		function function(x) {
				if (x === undefined) {
						return count;
				} else {
						count = count + x;
						return function;
				}
		}
return function(x);
}
